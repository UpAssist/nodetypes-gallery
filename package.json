{
  "name": "UpAssist.NodeTypes.Gallery",
  "description": "A Gallery nodetype for Neos",
  "license": "ISC",
  "private": true,
  "scripts": {
    "clean": "yarn rimraf ./Resources/Public",
    "make": "mkdir -p ./Resources/Public/styles",
    "styles": "cp ./Resources/Private/Fusion/Content/Gallery/Gallery.css ./Resources/Public/styles/Gallery.css",
    "scripts": "tsc --build",
    "build": "yarn clean && yarn make && yarn styles && yarn scripts"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
